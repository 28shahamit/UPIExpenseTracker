<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="@color/surface_alt">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/surface"
        app:title="Bank Accounts"
        app:titleTextColor="@color/on_surface"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:elevation="2dp"/>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="The default account is automatically debited whenever an expense is paid or logged. Balances are allowed to go negative."
                android:textSize="12.5sp"
                android:textColor="@color/on_surface_secondary"
                android:layout_marginBottom="16dp"/>

            <TextView
                android:id="@+id/tvNoAccounts"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="No accounts yet. Add one below - it'll become your default automatically."
                android:textColor="@color/on_surface_secondary"
                android:gravity="center"
                android:padding="24dp"
                android:visibility="gone"/>

            <LinearLayout
                android:id="@+id/llAccounts"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"/>

            <Button
                android:id="@+id/btnAddAccount"
                style="@style/Widget.Material3.Button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="ADD BANK ACCOUNT"
                android:minHeight="52dp"
                android:layout_marginTop="8dp"/>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
